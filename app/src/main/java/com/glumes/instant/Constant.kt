package com.glumes.instant

import android.Manifest

const val IMAGE_ACTION = "com.glumes.instant.image.shader"

const val CAMERA_ACTION = "com.glumes.instant.camera.shader"

val CAMERA_PERMISSION = Array(1) { Manifest.permission.CAMERA }